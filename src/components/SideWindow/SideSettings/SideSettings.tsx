import './SideSettings.scss';

export const SideSettings = () => {
    return (
        <div className="side-settings">
            <h2 className="side-settings__title">Settings</h2>
        </div>
    );
};
